# Copy to .env.local — never commit real keys
WANDB_API_KEY=
WANDB_ENTITY=chinmayks2013-student
WANDB_PROJECT=TravelRook

# Optional: WANDB_TRACE=false to disable Weave

# Optional: enables live OpenAI analysis for non-flight agents (fallback logic if unset)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
